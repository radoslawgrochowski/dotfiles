# radoslawgrochowski/dotfiles

This repository contains my personal dotfiles, managed using [nix](https://nixos.org/).
It provides a reproducible and declarative configuration for my development environment across different machines.

## Hosts

- `macaron` - nix-darwin macOS
- `radoslawgrochowski-wsl` - NixOS WSL

## Commands

Use `just` to list available commands and run them.
